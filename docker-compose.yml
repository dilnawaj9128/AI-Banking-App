version: '3.8'

services:

  mongo:
    image: mongo:7
    container_name: bankapp-mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
    networks:
      - bankapp-network
    restart: unless-stopped

  backend:
    build:
      context: ./backend
    container_name: bankapp-backend
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - JWT_SECRET=super-secret-jwt-key
      - NODE_ENV=production
      - MONGO_URI=mongodb://mongo:27017/bankapp
    depends_on:
      - mongo
    networks:
      - bankapp-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:5000/health"]
      interval: 30s
      timeout: 5s
      retries: 3

  frontend:
    build:
      context: ./frontend
      args:
        - VITE_API_URL=http://13.48.78.119:5000/api
    container_name: bankapp-frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
    networks:
      - bankapp-network
    restart: unless-stopped

volumes:
  mongo-data:

networks:
  bankapp-network:
    driver: bridge
